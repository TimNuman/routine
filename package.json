{
  "name": "routine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "lint": "oxlint",
    "format": "oxfmt",
    "check": "wrangler types --strict-vars=false && tsc --noEmit && oxlint && oxfmt --check",
    "backup": "node backup.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.121.0",
    "jose": "^6.2.10"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "hono": "^4.13.5",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.0.0"
  }
}
